# generator_file_line
This project demonstrates how to use Python's `yield` keyword to read a file line by line efficiently.   Instead of loading the entire file into memory, the generator delivers one line at a time and pauses until the next request.
